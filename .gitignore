# Dependencies
/node_modules

# Build output
/www/ # This is source, but capacitor copies it, so we might want to ignore the copy in some cases. For now, let's keep it.
/android

# Logs
*.log

# OS-generated files
.DS_Store
Thumbs.db
